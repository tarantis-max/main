# Local Jira credentials for the dashboard launcher (never commit your token)
dashboard/jira-token.dat
dashboard/jira-credentials.bat

# Site-specific custom-field IDs, generated by setup-jira-fields.js
dashboard/vpmo-fields.json

# React front end — dependencies and build output
app/node_modules/
app/dist/
